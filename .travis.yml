sudo: required
dist: trusty
language: r

r:
  - oldrel
  - release
  - devel

addons:
  apt:
    packages:
      - r-cran-rmpi
      - libssh2-1-dev

after_success:
  - Rscript -e 'library(covr);coveralls()'
